/** Reverse-DNS id Electrobun uses for the install root and update manifests. */
export const DESKTOP_IDENTIFIER = "team.toad.desktop";

/** Where installed builds look for `{channel}-{os}-{arch}-update.json`. */
export const RELEASE_BASE_URL = "https://toad.team/releases";

